import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <div style={{ textAlign: 'center', marginTop: '5rem', fontSize: '24px' }}>
      Welcome to <strong>Tregoza Marketplace</strong> 🚀
    </div>
  </React.StrictMode>
);